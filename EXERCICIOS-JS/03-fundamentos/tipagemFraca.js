let qualquer = 'Legal d+'
console.log(qualquer)
console.log(typeof qualquer)

qualquer = 3.1516
console.log(qualquer)
console.log(typeof qualquer)

// Podemos observar que JS é uma linguagem de tipagem fraca, ou seja, uma variável pode receber valores diferentes.

// Evitar nomes genéricos e siglas, coloque nomes que façam sentido para o você está implementando.
let valor = ''
let numero = 1
let pqp = false // Produto Químico Perigoso, Evite nomes como esses.